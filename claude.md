使用以下指令运行godot项目，让用户来游玩，你去检测后端产生的bug。
"E:\godot\Godot_v4.6.1-stable_win64.exe" --path  "f:\godot_game\扫雷_增量\saolei"

git提交的时候，不要添加Co-Authored-By相关的内容

建立计划的时候，一定要搜索调研一下其他游戏、项目是怎么做的。

优先用 Edit 工具直接编辑，不优先用 Python 补丁脚本
新增节点时考虑命名，避免 get_children() 遍历找节点时误匹配

使用worktree进行任务的时候，要用godot启动一次编辑器模式，才能启动游戏

## 玩法设计最高准则（2026-08-22 起）

**后续所有玩法设计（新机制/新关卡规则/新升级/新敌人/新特殊格子），一律以 `docs/active/玩法解构方法论-v1.md` 为准，不得绕过。**

- 任何新机制先过方法论 §5 验证四问（决策改变/可交互为否决项，体感断层/聚焦核心为质量分）
- 涉及"添乱者"实体（虫/敌人/污染源）必须过 §4 压力源设计六问
- 铁律：数字恒定不变量（已揭示数字与雷位永不变动）；自动化永不吞噬组合决策层（模糊推理留给玩家）
- 具体机制设计产出写入/引用 `docs/active/feature-spec-操作约束与自动化编排机制.md`，新维度候选看 `docs/active/扫雷独有维度-v1.md`
- 方法论允许演进：修订时更新版本号并在文档头记录变更，不静默改

## 视觉/图像模型联动守则（2026-08-14 贴图溢出事件教训）

- 视觉模型的结论只是方向性线索，不能作为"无问题/已修复"的判定证据；它会编造精确数字（如"24像素"），任何数字必须用像素工具独立验证
- 用户肉眼报告的问题 vs 模型/算法说"没差异"冲突时：默认相信用户，先怀疑自己的测量方法有盲区
- 像素级视觉问题（贴图拉伸/溢出/重叠）用 Python(numpy/PIL) 逐像素测量；测量前先输出中间结果（bbox、剖面、采样值）确认定位的对象正确，再下结论
- 测量算法不要预设结构（如"网格必须等距"），预设会把异常平均化掩盖；用独立峰检测 + 用户标注锚点交叉验证
- TextureRect 使用 AtlasTexture 时必须显式设置 expand_mode=1（默认 EXPAND_KEEP_SIZE 会把控件撑到纹理尺寸并向右下溢出）
- 详细复盘：docs/active/调试复盘-贴图溢出与视觉模型陷阱.md