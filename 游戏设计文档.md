# 扫雷挖矿 · 游戏设计文档

> **版本**：v0.1（原型设计阶段）
> **最后更新**：2026-08-03
> **项目目录**：`e:\godot\扫雷`

---

## 1. 概述

### 1.1 核心概念
将经典扫雷与自动化经营结合的 **60 秒积分制** 小游戏。玩家在扫雷地图上挖掘矿物（地雷），同时部署机器人协助自动清扫，构建"手动操作 + 自动化"的协同体验。

**一句话总结**：扫雷的规则、挖矿的味道、机器人帮忙、60 秒冲分。

### 1.2 玩家体验目标
- **前期（0-15s）**：纯扫雷节奏，玩家手点攒钱
- **中期（15-40s）**：第一个机器人进场，节奏开始自动化
- **后期（40-60s）**：多机器人协同，玩家专注做模糊推理和风险决策
- **整体曲线**：节奏越来越快、爽感越来越强

### 1.3 平台
- 引擎：Godot
- 视角：2D 俯视格子地图
- 操作：鼠标

---

## 2. 核心玩法

### 2.1 游戏目标
在 60 秒内尽可能获得高积分。提前清完所有安全格 = 胜利结算。

### 2.2 核心循环

```
玩家点击格子
   ↓
获得钱 + 积分
   ↓
攒钱买机器人
   ↓
机器人自动干活（开格 / 标雷）
   ↓
持续产出积分（被动收入）
   ↓
攒更多钱 → 买更多机器人 / 升级
   ↓
时间到 → 结算积分
```

### 2.3 胜利条件
- **提前清完**：所有非雷格子被打开 → 立即胜利结算
- **撑到时间到**：60 秒结束时按当前积分结算

### 2.4 失败 / 结束条件
- **命用完**：3 条命归零 → 立即失败结算（保留已得积分）
- **时间到未清完**：按当前积分结算（不算失败，但分数受限）

---

## 3. 地图与基础机制

### 3.1 地图规格

| 项 | 值 |
|---|---|
| 网格大小 | 16 × 16 = 256 格 |
| 地雷数 | 40 个（密度 ~15.6%） |
| 安全格数 | 216 个 |
| 预开区域 | 中心 3 × 3（9 格） |

### 3.2 格子类型

| 类型 | 描述 | 视觉 |
|---|---|---|
| 关闭（墙） | 未挖掘的墙，未知状态 | 深色方块（岩石感） |
| 打开 - 空 | 周围 8 格无雷 | 浅色底（泥土感） |
| 打开 - 数字 | 周围 8 格有 N 个雷（1-8） | 数字（按经典扫雷配色） |
| 标记（旗） | 玩家或机器人标记为雷 | 旗子图标 |
| 坍塌 | 玩家踩雷后的格子 | 暗红 / 裂纹（不能再操作） |

### 3.3 数字逻辑
打开的格子显示其周围 8 个邻格中地雷的数量（0-8）。**0 时显示为空白，并自动连锁打开所有相连的 0 格子**（标准扫雷规则）。

### 3.4 预开区域
- 中心 3 × 3 在关卡开始时已打开
- 给玩家一个起步的"已挖区域"，能立即往外扩或放机器人
- 地雷生成时避开预开区域（保证开局不立刻踩雷）

### 3.5 地雷生成规则
1. 初始化 16×16 全关闭
2. 标记中心 3×3 为"预开区域"
3. 在剩余 247 格中随机选 40 个放雷
4. 计算每个非雷格子的相邻雷数
5. 打开预开区域的 9 格（应用连锁展开规则）

---

## 4. 玩家操作

### 4.1 左键：开格子
- 点关闭格子 → 打开
- 如果是雷 → 扣 1 命，该格子变为"坍塌"状态
- 如果是数字 / 空 → 正常显示，空格连锁展开

### 4.2 右键：标雷
- 点关闭格子 → 切换标记 / 取消标记
- **标对一个雷** → +5 钱 +5 分
- **标错一个雷**（实际不是雷）→ -3 分（不扣钱）
  - 标错的格子不会自动取消，玩家可手动右键取消
- 已打开格子不能标记

### 4.3 双击：和弦（Chord）
- 双击数字格子
- 若该格子周围**所有雷都已被正确标记**，自动打开剩余的所有安全邻格
- 若标记数 ≠ 数字 或标错，无效果
- 老玩家的高效技巧，扫雷经典操作

### 4.4 操作限制
- 玩家不能在已有机器人的格子上放机器人（一格一机）
- 玩家可以正常开 / 标格子（即使有机器人在邻格）

---

## 5. 经济系统

### 5.1 双货币系统

| 货币 | 用途 | 来源 | 关卡间 |
|---|---|---|---|
| **钱** | 关卡内消耗（买机器人、升级） | 玩家行动、机器人行动 | 不保留 |
| **积分** | 纯成绩，不可消耗 | 玩家行动、机器人行动 | 最终成绩 |

**关键设计**：钱和积分完全独立。**花钱买机器人不会降低积分**。

### 5.2 起始资源

| 项 | 起始值 |
|---|---|
| 钱 | 50（够立刻买 1 个机器人） |
| 积分 | 0 |
| 命 | 3 |
| 时间 | 60 秒 |
| 机器人 | 0（不送，需自己买） |

### 5.3 行动奖励表

| 行动 | 钱 | 积分 | 备注 |
|---|---|---|---|
| 玩家开一格（安全） | +1 | +1 | |
| 玩家标对一个雷 | +5 | +5 | "挖矿"高回报 |
| 玩家标错一个雷 | 0 | -3 | 不扣钱，避免雪上加霜 |
| 玩家踩雷 | 0 | 0 | -1 命 |
| 机器人开一格 | +1 | +1 | 被动收入 |
| 机器人标对一个雷 | +5 | +5 | 被动收入 |

### 5.4 踩雷后果
- 扣 1 命
- 该格子变为"坍塌"状态（视觉上不同，不能再开 / 标，但视为已打开）
- 不影响其他格子，游戏继续

---

## 6. 机器人系统

### 6.1 机器人类型

| 类型 | 颜色 | 图标 | 行为 |
|---|---|---|---|
| **开墙型** | 蓝色 | 镐头 | 打开确定安全的墙体 |
| **标雷型** | 黄色 | 旗子 | 标记确定是雷的墙体 |

**严格类型分工**：开墙型只开墙，标雷型只标雷，不会跨界作业。

### 6.2 行为规则

#### 判定逻辑（核心）
机器人**只执行 100% 确定的操作**，基于扫雷推理规则：

- **规则 1（可开）**：一个数字格子，其周围**已标记的雷数 = 数字** → 其余未开邻格都安全，可开
- **规则 2（可标）**：一个数字格子，其周围**未开邻格数 = 数字** → 这些邻格全是雷，可标

机器人不会执行需要概率判断或猜测的操作。

> 这两条规则覆盖约 90% 的局面。剩下的"必须猜"环节是设计预期，机器人会卡住，需要玩家用命硬闯——这是玩家存在的价值。

#### 作业节奏
- 每隔 2 秒（基础速度）执行一次动作
- 一次动作 = 移动一格 或 开墙/标雷
- 升级速度可缩短至 1.5s → 1.0s

### 6.3 寻路与协调

#### 寻路
- 机器人**只能在已开格子上行走**（关闭的墙是障碍）
- 使用 **BFS** 在已开区域找最近的可作业墙
- 不需要复杂 A*，BFS 足够

#### 多机协调
- 系统维护一个全局"已锁定目标"集合
- 机器人扫描目标时，跳过已被其他机器人锁定的目标
- 实现**先到先得**效果

#### 空闲行为
- 若全图没有可执行操作 → 机器人原地待命
- 当玩家或机器人开新格子产生新数字 → 触发重新扫描

### 6.4 机器人购买

#### 成本递增（防止雪球失控）

| 第 N 个 | 价格 |
|---|---|
| 1 | 50 |
| 2 | 100 |
| 3 | 200 |
| 4 | 400 |
| 5 | 800 |
| ... | 50 × 2^(N-1) |

第 5 个 800 块基本买不起，相当于软上限。

#### 购买流程
1. 玩家点击商店中的机器人类型按钮
2. 鼠标变为"放置模式"（光标改变）
3. 玩家在任意**已开格子上**点击 → 机器人出现
4. 钱扣除
5. ESC 或右键取消放置

#### 召回政策
**不支持免费召回**。机器人一旦放置，由系统自动调度。
> 设计意图：让"放哪里、放几个"成为策略决策，而不是无脑试错。

### 6.5 升级系统

**全局升级**：升级一次，所有同类机器人都受益。

| 升级项 | Lv 0 | Lv 1 | Lv 2 | 价格 |
|---|---|---|---|---|
| 开墙型速度 | 2.0s | 1.5s | 1.0s | 100 / 200 |
| 标雷型速度 | 2.0s | 1.5s | 1.0s | 100 / 200 |
| 购买折扣 | 0% | -25% | -50% | 200 / 500 |

> 具体数值待原型测试后调整。

---

## 7. 关卡结构

### 7.1 当前版本范围
- **单关原型**（先做一关看效果）
- 多关闯关、难度递增、跨关继承为后续扩展方向

### 7.2 关卡参数

| 项 | 值 |
|---|---|
| 时长 | 60 秒 |
| 命数 | 3（每关独立，不跨关） |
| 起始钱 | 50 |
| 起始机器人 | 0 |

---

## 8. UI/UX 设计

### 8.1 HUD 布局（建议）

```
+----------------------------------------------------+
|  钱: 150   积分: 230   命: 3/3   时间: 0:42       |
+----------------------------------------------------+
|                                                    |
|                                                    |
|              [16 × 16 游戏地图]                    |
|                                                    |
|                                                    |
|                                                    |
+----------------------------------------------------+
|  [买开墙型 ¥50]  [买标雷型 ¥50]  [升级面板]       |
+----------------------------------------------------+
```

### 8.2 商店 UI
- 底部按钮栏
- 鼠标悬停显示价格和描述
- 点击进入"放置模式"
- 钱不够时按钮变灰

### 8.3 升级面板
- 弹出式面板（点击"升级面板"按钮触发）
- 列出所有可升级项
- 显示当前等级、下一级效果、价格
- 不可升级时显示"已满级"

### 8.4 视觉风格
- **整体**：像素风或简洁扁平风（待定）
- **已开区域**：暖色（土黄 / 棕色，泥土感）
- **关闭墙体**：深色（深棕 / 灰，岩石感）
- **数字**：经典扫雷配色（1 蓝、2 绿、3 红、4 深紫...）
- **机器人**：醒目色彩，移动有平滑动画，作业时有小动画

---

## 9. 实现要点

### 9.1 扫雷求解器（机器人核心）
输入当前地图状态，输出所有 100% 确定的操作。

**伪代码**：
```python
def find_certain_actions(board):
    actions = []
    for cell in board.opened_cells:
        if cell.number == 0:
            continue
        neighbors = board.get_neighbors(cell)
        flagged = [n for n in neighbors if n.is_flagged]
        unopened = [n for n in neighbors if n.is_unopened]

        # 规则 1：所有雷已标 → 其余可开
        if len(flagged) == cell.number:
            for n in unopened:
                actions.append(("open", n))

        # 规则 2：未开邻格数 = 剩余雷数 → 全是雷
        if len(unopened) + len(flagged) == cell.number:
            for n in unopened:
                actions.append(("flag", n))
    return actions
```

### 9.2 寻路实现

```python
def find_nearest_work(robot, work_targets, locked_targets):
    available = [t for t in work_targets if t not in locked_targets]
    if not available:
        return None

    # BFS on opened cells only
    queue = [(robot.position, 0, [])]
    visited = {robot.position}
    while queue:
        pos, dist, path = queue.pop(0)
        for target in available:
            if is_adjacent(pos, target):
                return (target, path)
        for n in board.opened_neighbors(pos):
            if n not in visited:
                visited.add(n)
                queue.append((n, dist + 1, path + [n]))
    return None
```

### 9.3 多机器人协调

```python
# 全局状态
locked_targets = set()

def robot_tick(robot):
    actions = find_certain_actions(board)
    targets = extract_targets(actions)

    result = find_nearest_work(robot, targets, locked_targets)
    if result is None:
        return  # 待命

    target, path = result
    locked_targets.add(target)

    if not path:
        # 邻接，直接作业
        execute(robot, target)
        locked_targets.discard(target)
    else:
        # 移动一步
        robot.position = path[0]
```

### 9.4 主循环（每帧）
```
1. 处理玩家输入（点击、放置机器人、商店、升级）
2. 更新计时器（每秒 -1）
3. 触发机器人 tick（按各机器人的速度间隔）
4. 检查胜利 / 失败条件
5. 渲染
```

### 9.5 数据结构建议

```python
class Cell:
    x: int
    y: int
    is_mine: bool
    is_opened: bool
    is_flagged: bool
    is_collapsed: bool  # 踩雷坍塌
    number: int         # 0-8

class Robot:
    x: int
    y: int
    type: RobotType     # OPENER / MARKER
    last_tick_time: float
    current_target: Cell
```

---

## 10. 完整数值表（速查）

| 项 | 值 |
|---|---|
| 地图大小 | 16 × 16 |
| 地雷数 | 40 |
| 预开格 | 中心 3 × 3 |
| 关卡时长 | 60 秒 |
| 起始命 | 3 |
| 起始钱 | 50 |
| 玩家开格 | +1 钱 +1 分 |
| 玩家标对雷 | +5 钱 +5 分 |
| 玩家标错雷 | -3 分（不扣钱） |
| 玩家踩雷 | -1 命 |
| 机器人开格 | +1 钱 +1 分 |
| 机器人标对雷 | +5 钱 +5 分 |
| 机器人速度（基础） | 2.0 秒 / 次 |
| 机器人价格 | 50 / 100 / 200 / 400 / 800 ... |
| 速度升级 | 2.0s → 1.5s → 1.0s |
| 购买折扣升级 | 0% → 25% → 50% |

---

## 11. 未来扩展方向（暂不实现）

- **多关闯关**：地图变大、雷密度增加、特殊地块
- **特殊地块**：无法开的硬墙、价值更高的稀有雷、爆炸雷（连锁伤害）
- **新机器人类型**：
  - 探测型（揭示未知区域属性）
  - 收益加成型（机器人开的格子奖励翻倍）
- **Meta 进度**：跨关保留升级、解锁新机器人类型
- **挑战模式**：每日关卡、不踩雷通关、极限速通

---

## 12. 已知设计风险

### 12.1 雪球失控
机器人给钱给分，前 30 秒攒了 3 个机器人的玩家后 30 秒可能爆发增长。
- **缓解**：成本翻倍递增（第 5 个 800 块，几乎买不起）
- **待实测调整**

### 12.2 机器人挡视线
多个机器人在地图上走来走去，可能干扰玩家阅读局面。
- **缓解**：机器人半透明 / 体积小 / 提供关闭显示选项

### 12.3 求解器卡死时玩家迷茫
机器人不动了，玩家不知道是不是 bug。
- **缓解**：UI 提示"机器人无可执行操作"，引导玩家手动破局

### 12.4 开局选择压力
玩家开场 50 块，必须立刻决定买开墙型还是标雷型，可能选择困难。
- **缓解**：商店按钮上写清楚两类的作用和适用场景
- **替代方案**：起始送 1 个固定开墙型，玩家首次购买压力减小

---

## 13. 实现阶段（建议顺序）

| 阶段 | 内容 | 优先级 |
|---|---|---|
| **P1** | 扫雷核心（网格、渲染、左右键、数字、连锁展开、胜利检测） | 必须 |
| **P2** | 状态结算（钱、积分、命、计时器 UI、胜利/失败面板） | 必须 |
| **P3** | 和弦操作、商店 UI（购买 + 放置） | 必须 |
| **P4** | 机器人系统（求解器、寻路、协调、动画） | 必须 |
| **P5** | 升级系统（全局面板、速度/折扣升级） | 完整体验 |
| **P6** | 视觉打磨（颜色图标、动画、特效） | 打磨版 |

**MVP** = P1 + P2 + P3 + P4

---

## 附录：术语表

| 术语 | 含义 |
|---|---|
| 墙 / 关闭格 | 未打开的格子，对机器人和玩家都是未知 |
| 开格子 | 把关闭格变为打开状态 |
| 标雷 | 在关闭格上放旗子，标记为雷 |
| 和弦 | 双击数字格，自动开完其周围所有安全邻格 |
| 坍塌 | 踩雷后的格子状态，不能再操作 |
| 开墙型机器人 | 蓝色 / 镐头，专门打开确定安全的墙 |
| 标雷型机器人 | 黄色 / 旗子，专门标记确定是雷的墙 |
| 求解器 | 计算所有 100% 确定操作的算法 |
| 雪球 | 资源指数级增长的失控现象 |
