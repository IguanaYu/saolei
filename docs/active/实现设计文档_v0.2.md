# 扫雷挖矿 v0.2 实现设计文档

## Context

v0.1 已实现扫雷核心 + opener/marker 两种机器人 + 局内升级，但只有"自动扫雷"单线玩法，缺乏深度和主题感。

v0.2 引入**矿脉产业链**（标雷 → 检测 → 矿脉 → 采矿 → 运输 → 结算）+ **建筑系统**（基地、充能塔）+ **一次性技能**（无人机）+ **局外成长**（矿石解锁），把单局小游戏扩展为可反复挑战的增量游戏。

本设计文档是开发者落地指南，覆盖 P6-P11 六个阶段的实现细节。**实施时按 MVP 优先：P6（基地）→ P7（检测型+矿脉）→ P8（矿工）必须先完成**，再做 P9-P11。

---

## 关键设计决策

| 决策点 | 选择 | 理由 |
|---|---|---|
| detector/miner 实现 | **继承 Robot 基类**，独立 .tscn 场景 | 复用移动逻辑、buff、`robot_manager.tick_all` 统一调度；GDScript 多态直接覆盖 `do_tick` |
| 充能塔/buf 叠加 | **乘法叠加**：`effective_interval = base_interval / speed_mult` | 局外永久升级（改 base）+ 临时 buff（改 multiplier）正交，buff 到期无需恢复 |
| 存档方案 | **FileAccess + JSON**，路径 `user://save_data.json` | 嵌套 unlocks 字典用 ConfigFile 不便；JSON 含 `version` 字段便于兼容性 |
| 第一个基地放置 | **玩家点击任意关闭格 → 该格变基地 + 周围 3×3 自动预开 + 触发雷生成** | 新手友好；放置即开局，无需"先开格再放基地"两步 |
| detector 的 3 秒 | **检测时长**（到达旗子邻接格后停留 3 秒判定）；移动速度用通用 2s/格 | 用户原话"移动和大家一样，检测很慢 3 秒"明确区分 |
| miner 不锁目标 | 矿工不用 `locked_targets`，多矿工可叠采同一矿脉 | 加速耗尽，符合"雪球"设计；寻路仍用 `robot_at` 防互踩 |
| 矿工结算时机 | **卸货到基地瞬间结算**（装 cargo 时不结算） | 让"运输"有意义；cargo 满 20 才回基地 |
| 索引优化 | v0.2 **不做**旗子/矿脉索引缓存，每次扫描 256 格（< 0.1ms） | 简单优先，profile 后再优化 |

---

## 1. 架构扩展

### 1.1 新增类

| 类 | 路径 | extends | 职责 |
|---|---|---|---|
| `DetectorRobot` | `scripts/robots/detector_robot.gd` | `Robot` | 找旗子→移动→3 秒检测→成功变矿脉/失败自爆 |
| `MinerRobot` | `scripts/robots/miner_robot.gd` | `Robot` | 四态状态机：to_mine/mining/to_base/unloading |
| `SaveSystem` | `scripts/autoload/save_system.gd` | `Node`（autoload 单例） | 矿石 + 解锁状态的 JSON 持久化 |

**不做成独立类**：Base / Vein / ChargeTower / Drone 都是 **Cell 的属性标记 + Grid/GameState 管理逻辑**（符合 v0.1 "Cell 是一切网格内容的容器"模式）。

#### DetectorRobot 核心字段

```
var _detecting_timer: float = 0.0     # 检测计时（独立于 tick_interval）
var _detecting_target: Variant = null  # 正在检测的旗子坐标
```

行为分两段：
- 移动阶段：复用基类 `accumulate_and_maybe_tick` + `do_tick`（tick_interval = 通用 2s）
- 检测阶段：覆盖 `_process(delta)`，累计 `_detecting_timer >= 3.0` 时调用 `_resolve_detection()`

#### MinerRobot 核心字段

```
const CARGO_CAPACITY: int = 20
const MINE_PER_TICK: int = 5
var cargo: int = 0
var miner_state: String = "to_mine"   # to_mine | mining | to_base | unloading
var _current_vein_coord: Variant = null
```

#### SaveSystem 字段

```
const SAVE_PATH := "user://save_data.json"
var ore: int = 0
var unlocks: Dictionary = {
    "start_money": 0, "start_lives": 0, "global_speed": 0,
    "expand_zone": 0, "start_robot": 0,
    "detector": false, "miner": false,
    "charge_tower": false, "drone": false,
}
```

API: `load_game() / save_game() / spend_ore(amount) -> bool / purchase_unlock(key) -> bool`

### 1.2 修改现有类

#### `scripts/autoload/game_state.gd`（大改）

新增字段：
- `bases: Array[Vector2i]` / `charge_towers: Array[Vector2i]`
- `base_count / charge_tower_count / detector_count / miner_count: int`
- `detector_speed_level / miner_speed_level: int`
- `game_phase: String`（"placing_base" | "playing"）

新增信号：
- `base_placed(coord)` / `charge_tower_placed(coord)`
- `vein_created(coord)` / `vein_depleted(coord)`
- `cargo_changed(robot, cargo)` / `robot_destroyed(robot)`
- `game_phase_changed(phase)`

修改 `reset_state()`：起始值改为 `money=100, lives=3, time_left=90`，从 SaveSystem 读起始加成
修改 `get_robot_price(type)`：扩展 4 种类型基础价（opener/marker=50, detector=80, miner=60），翻倍递增
修改 `get_speed_interval(type)`：detector 用 `[3.0, 2.5, 2.0]`，其他用 `[2.0, 1.5, 1.0]`；乘局外速度系数

新增：`get_base_price() / get_charge_tower_price() / register_base(coord) / get_nearest_base(coord) -> Variant`

#### `scripts/grid.gd`（大改）

关键改动：**`_ready()` 不再立即生成雷**，改为：
1. `_ready()` 只创建 256 个空 Cell（`init_empty_grid()`）
2. `game_phase = "placing_base"`
3. 玩家放第一个基地 → `place_first_base(coord)` 触发雷生成 + 预开

新增方法：
- `init_empty_grid()` - 创建空网格
- `place_first_base(coord)` - 第一个基地特例（任意关闭格可放，触发雷生成）
- `place_base(coord)` / `place_charge_tower(coord)` - 后续建筑（必须在已开格）
- `get_all_flagged_cells() -> Array` - detector 扫描用
- `get_all_veins() -> Array` - miner 扫描用
- `get_farthest_closed_cells(n, base_coord) -> Array` - 无人机用
- `_pre_open_area(coords)` - 预开区域

修改 `open_cell(coord, by_actor, give_reward=true)`：无人机调用传 false
修改 `is_walkable()`：基地/矿脉/充能塔视为可通行
修改 `mine_count: int = 32`（从 40 改）

`map_generator.gd` 新增 `generate_excluding(rows, cols, mine_count, exclude_coords: Array)` 静态方法。

#### `scripts/cell.gd`（中改）

新增字段：`is_vein / vein_resources / is_base / is_charge_tower`
新增方法：`become_vein(resources) / deplete_vein() / become_base() / become_charge_tower()`
修改 `open()`：建筑格不可被开
修改 `refresh_visual()`：优先级链 `base > charge_tower > vein > collapsed > flagged > opened > closed`

#### `scripts/robot.gd`（小改）

新增字段：`buff_until: float = 0.0` / `speed_mult: float = 1.0`
修改 `accumulate_and_maybe_tick(delta, ...)`：

```
var base_interval := GameState.get_speed_interval(robot_type)
var effective: float = base_interval
if Time.get_ticks_msec() / 1000.0 < buff_until:
    effective = base_interval / speed_mult
_tick_timer += delta
if _tick_timer >= effective:
    _tick_timer = 0.0
    do_tick(grid, locked, robot_positions)
```

新增方法：`apply_buff(duration, mult)` / `is_buffed() -> bool`
**抽取 `do_tick` 中"移动一步"逻辑为 `_move_step(grid, robot_positions, targets) -> bool`**，供 MinerRobot 复用。

#### `scripts/robot_manager.gd`（中改）

修改 `spawn_robot(coord, type, grid)`：根据 type 实例化 DetectorRobot.tscn / MinerRobot.tscn / Robot.tscn
新增 `remove_robot(robot, reason)`：detector 自爆用，先 `robots.erase` 再 `queue_free`，emit `robot_removed`
新增 `tick_charge_towers(delta, grid)`：每帧调用，管理 `_tower_cooldowns: Dictionary`
新增 `_find_robots_in_range(center, radius, grid) -> Array`
修改 `tick_all(delta, grid)`：末尾加 `tick_charge_towers(delta, grid)`

#### `scripts/main.gd`（中改）

字段：`placing_robot_type` 改为 `placing_mode: String`（值域扩展到 opener/marker/detector/miner/base/charge_tower）
新增 `_trigger_drone()`：即时扣钱 + 调用 `grid.get_farthest_closed_cells` + 逐个 `open_cell(coord, "drone", false)`
修改 `_ready()`：加入初始基地放置阶段（拦截其他输入直到放完基地）
修改 `_try_place_at()`：分支处理机器人/建筑/初始基地
新增 `_on_robot_removed(robot, reason)`：reason=="detect_failed" 时调 `hud.show_toast(...)`

修改 `_on_cell_opened(_cell, by_actor)`：`by_actor == "drone"` 时不给奖励

#### `scripts/ui/shop.gd`（中改）

- 拆为 `RobotRow`（4 个机器人按钮）+ `BuildRow`（基地/塔/无人机/升级）
- 新增 `_is_unlocked(type) -> bool`：查 `SaveSystem.unlocks`
- 未解锁按钮显示"🔒"，disabled
- 新增 `_enter_build_mode(type)` 和 `_trigger_drone()`

#### `scripts/ui/upgrade_panel.gd`（大改）

新增 2 行局内升级：`DetectorSpeedRow / MinerSpeedRow`
新增 9 行局外升级（用矿石买）：`StartMoneyRow / StartLivesRow / GlobalSpeedRow / ExpandZoneRow / StartRobotRow / UnlockDetectorRow / UnlockMinerRow / UnlockTowerRow / UnlockDroneRow`
分区用 HSeparator + 子标题隔开

#### `scripts/ui/hud.gd`（小改）

新增 `OreLabel` / `PhaseHintLabel` / `ToastLabel`
新增 `show_toast(text, duration=3.0)`：Tween 淡出
新增 `set_phase_hint(phase)`：placing_base 显示提示

#### `scripts/ui/results_panel.gd`（小改）

结算时计算 `ore_earned = score / 10`，调用 `SaveSystem.ore += ore_earned; SaveSystem.save_game()`，UI 显示"+X 矿石"

### 1.3 场景文件改动

| 文件 | 改动 |
|---|---|
| `scenes/Cell.tscn` | 新增 `VeinSprite(Label)` / `BuildingSprite(Label)` 子节点，默认隐藏 |
| `scenes/Robot.tscn` | 无改动（子类共享） |
| `scenes/DetectorRobot.tscn`（新） | 继承 Robot.tscn，改 script + Body 紫色 + IconLabel "🔍" |
| `scenes/MinerRobot.tscn`（新） | 继承 Robot.tscn，改 script + Body 橙色 + IconLabel "⛏" |
| `scenes/ui/Shop.tscn` | HBoxContainer → 改名 RobotRow，4 个按钮；新增 BuildRow，4 个按钮（基地/塔/无人机/升级） |
| `scenes/ui/HUD.tscn` | HBoxContainer 加 OreLabel；新增 PhaseHintLabel + ToastLabel |
| `scenes/ui/UpgradePanel.tscn` | 新增 DetectorSpeedRow / MinerSpeedRow + HSeparator + 9 行局外升级 |
| `scenes/Main.tscn` | 无节点改动（SaveSystem 是 autoload） |
| `project.godot` | `[autoload]` 加 `SaveSystem="*res://scripts/autoload/save_system.gd"` |

### 1.4 关键可复用 API（不重写）

| 函数 | 文件:行 | 复用场景 |
|---|---|---|
| `Pathfinding.find_nearest_target` | pathfinding.gd:15 | detector/miner 寻路（矿工传空 locked） |
| `Grid.get_neighbors(coord)` | grid.gd:63 | 求解器、邻接判定 |
| `Grid.coord_to_world / world_to_coord` | grid.gd:137-145 | 机器人移动 |
| `Solver.find_certain_actions` | solver.gd:7 | opener/marker（不变） |
| `Robot._move_to / set_initial_position` | robot.gd:23-25, 113 | 子类直接继承 |
| `GameState.locked_targets` | game_state.gd:22 | opener/marker/detector 共用 |

---

## 2. 关键算法

### 2.1 基地放置 + 雷生成时序

```
_ready() → init_empty_grid()（256 关闭格）→ game_phase="placing_base"
玩家点击任意格 → _try_place_at() 检测到 phase=placing_base
   ↓
grid.place_first_base(coord):
   1. 计算 safe_radius = 1 + SaveSystem.unlocks["expand_zone"]
   2. 收集 safe_coords = coord + 周围方形区域
   3. data = MapGenerator.generate_excluding(16, 16, 32, safe_coords)
   4. 填充每个 cell.is_mine / adjacent_mines
   5. 对 safe_coords 中每个 cell.open("system")（预开）
   6. cells[coord].become_base()
   7. GameState.register_base(coord)
   8. game_phase = "playing"
   9. 如果 SaveSystem.unlocks["start_robot"]>=1 → spawn opener
   10. 如果 >=2 → 再 spawn marker
```

边界处理：`safe_coords` 用 `cells.has(sc)` 过滤超出地图的格子。

### 2.2 检测型机器人（移动 + 检测两阶段）

```
DetectorRobot.do_tick(grid, locked, robot_positions):
    if _state == "detecting":
        return  # 由 _process 处理计时
    # 清理失效目标（旗子被取消 / 已变矿脉）
    if _current_target != null:
        var c = grid.get_cell(_current_target)
        if c == null or not c.is_flagged or c.is_vein:
            locked.erase(_current_target); _current_target = null
    # 找旗子
    if _current_target == null:
        var flagged := grid.get_all_flagged_cells().filter(
            func(c): return not locked.has(c))
        if flagged.is_empty(): _state = "idle"; return
        var found := Pathfinding.find_nearest_target(
            grid, coord, flagged, locked, robot_positions, self)
        if found.is_empty(): _state = "idle"; return
        _current_target = found.target; locked[_current_target] = self
    # 移动一步 / 到达转 detecting
    var path := Pathfinding.find_nearest_target(
        grid, coord, [_current_target], locked, robot_positions, self)
    if path.work_pos == coord:
        _state = "detecting"; _detecting_target = _current_target
        _detecting_timer = 0.0
    else:
        _move_to(path.path[0], grid); coord = path.path[0]

DetectorRobot._process(delta):
    if _state != "detecting": return
    _detecting_timer += delta
    if _detecting_timer >= GameState.get_speed_interval("detector"):
        _resolve_detection()

_resolve_detection():
    var cell = grid.get_cell(_detecting_target)
    if cell == null or not cell.is_flagged:
        _state = "idle"; _detecting_target = null; _current_target = null; return
    if cell.is_mine:
        GameState.add_score(10)
        cell.become_vein(100)
        GameState.locked_targets.erase(_detecting_target)
        grid.vein_created.emit(cell.coord)
        _state = "idle"; _detecting_target = null; _current_target = null
    else:
        GameState.locked_targets.erase(_detecting_target)
        get_node("/root/Main/RobotManager").remove_robot(self, "detect_failed")
```

### 2.3 矿工四态状态机

```
状态转换图：
  to_mine ──cargo>=20──> to_base
  to_mine ──到达矿脉──> mining
  mining ──cargo>=20──> to_base
  mining ──矿脉耗尽──> to_mine
  to_base ──到达基地──> unloading
  unloading ──立即──> to_mine

MinerRobot.do_tick(grid, locked, robot_positions):
    match miner_state:
        "to_mine":
            if _current_vein_coord == null \
               or grid.get_cell(_current_vein_coord).vein_resources <= 0:
                var veins := grid.get_all_veins()
                if veins.is_empty(): _state = "idle"; return
                var found := Pathfinding.find_nearest_target(
                    grid, coord, veins, {}, robot_positions, self)
                if found.is_empty(): _state = "idle"; return
                _current_vein_coord = found.target
            _move_step(grid, robot_positions, [_current_vein_coord])
            if _at_work_pos: miner_state = "mining"

        "mining":
            var v = grid.get_cell(_current_vein_coord)
            if v == null or v.vein_resources <= 0:
                v.deplete_vein(); grid.vein_depleted.emit(v.coord)
                _current_vein_coord = null; miner_state = "to_mine"; return
            cargo += MINE_PER_TICK; v.vein_resources -= MINE_PER_TICK
            GameState.cargo_changed.emit(self, cargo)
            if cargo >= CARGO_CAPACITY:
                miner_state = "to_base"

        "to_base":
            if GameState.bases.is_empty(): _state = "idle"; return
            var found := Pathfinding.find_nearest_target(
                grid, coord, GameState.bases, {}, robot_positions, self)
            if found.is_empty(): _state = "idle"; return
            _move_step(grid, robot_positions, [found.target])
            if _at_work_pos: miner_state = "unloading"

        "unloading":
            GameState.add_money(cargo); GameState.add_score(cargo)
            cargo = 0
            GameState.cargo_changed.emit(self, 0)
            miner_state = "to_mine"
            _play_action_pulse()
```

注意：`_move_step` 是从 Robot 基类 `do_tick` 抽取的新方法，返回 `_at_work_pos: bool`。

### 2.4 充能塔 buff 系统

充能塔不是 Node，是 Cell 标记 + GameState 坐标列表 + robot_manager.tick_charge_towers 调度。

```
RobotManager.tick_charge_towers(delta, grid):
    for tower_coord in GameState.charge_towers:
        _tower_cooldowns[tower_coord] = \
            _tower_cooldowns.get(tower_coord, 0.0) - delta
        if _tower_cooldowns[tower_coord] <= 0.0:
            var in_range := _find_robots_in_range(tower_coord, 2)
            if not in_range.is_empty():
                in_range.pick_random().apply_buff(5.0, 1.5)
                _tower_cooldowns[tower_coord] = 5.0

_find_robots_in_range(center, radius):
    return robots.filter(func(r):
        return abs(r.coord.x - center.x) <= radius \
           and abs(r.coord.y - center.y) <= radius)
```

Buff 应用见 1.2 robot.gd 改动（乘法叠加到 effective_interval）。

### 2.5 无人机目标选择

**定义"最远"= 离最近基地曼哈顿距离最远**（基地是机器人作业中心，远=盲区）。

```
Grid.get_farthest_closed_cells(n, base_coord):
    var closed := []
    for coord in cells:
        var c = cells[coord]
        if not c.is_opened and not c.is_flagged and not c.is_mine \
           and not c.is_base and not c.is_charge_tower:
            closed.append({"coord": coord, "dist": \
                abs(coord.x - base_coord.x) + abs(coord.y - base_coord.y)})
    closed.sort_custom(func(a, b): return a.dist > b.dist)
    return closed.slice(0, n).map(func(e): return e.coord)

main._trigger_drone():
    if GameState.money < 100: return
    GameState.add_money(-100)
    var base = GameState.bases[0] if not GameState.bases.is_empty() \
               else Vector2i(8, 8)
    for coord in grid.get_farthest_closed_cells(3, base):
        grid.open_cell(coord, "drone", false)  # 不给奖励
```

### 2.6 存档系统

```
SaveSystem.load_game():
    if not FileAccess.file_exists(SAVE_PATH): return
    var f = FileAccess.open(SAVE_PATH, FileAccess.READ)
    var data: Variant = JSON.parse_string(f.get_as_text())
    if typeof(data) != TYPE_DICTIONARY: push_warning("存档损坏"); return
    ore = int(data.get("ore", 0))
    var saved: Dictionary = data.get("unlocks", {})
    for key in unlocks:  # 逐字段合并，兼容旧存档
        if saved.has(key): unlocks[key] = saved[key]

SaveSystem.save_game():
    var data = {"version": 2, "ore": ore, "unlocks": unlocks}
    var f = FileAccess.open(SAVE_PATH, FileAccess.WRITE)
    f.store_string(JSON.stringify(data, "  "))
```

兼容性：未来加新解锁项只需在 `unlocks` 字典加默认值，旧存档读取自动补全。

---

## 3. 信号流总表

| 信号源 | 信号 | 接收方 | 作用 |
|---|---|---|---|
| GameState | `game_phase_changed(phase)` | HUD | 显示/隐藏"请放基地"提示 |
| GameState | `base_placed(coord)` / `charge_tower_placed(coord)` | main | 日志/特效（可选） |
| Grid | `vein_created(coord)` / `vein_depleted(coord)` | main | 飘字提示（可选） |
| RobotManager | `robot_removed(robot, reason)` | main → HUD | reason=="detect_failed" 时 show_toast |
| SaveSystem | `ore_changed(new_ore)` | HUD / UpgradePanel | 刷新矿石显示 |
| SaveSystem | `unlock_purchased(key)` | Shop / UpgradePanel | 刷新按钮锁定/行状态 |
| GameState | `cargo_changed(robot, cargo)` | （预留，可做矿工头上数字） | — |

**HUD.show_toast 实现**：

```
func show_toast(text: String, duration: float = 3.0) -> void:
    toast_label.text = text
    toast_label.visible = true
    toast_label.modulate.a = 1.0
    var t := create_tween()
    t.tween_interval(duration)
    t.tween_property(toast_label, "modulate:a", 0.0, 0.5)
    t.tween_callback(func(): toast_label.visible = false)
```

---

## 4. 分阶段实现

### P6：基地系统

**改动文件**：
- `scripts/autoload/game_state.gd`（bases/base_count/game_phase/register_base/get_base_price）
- `scripts/grid.gd`（init_empty_grid/place_first_base/place_base）
- `scripts/map_generator.gd`（generate_excluding）
- `scripts/cell.gd`（is_base/become_base/refresh_visual 分支）
- `scripts/main.gd`（placing_mode 扩展 + 初始基地阶段）
- `scripts/ui/hud.gd` + `scenes/ui/HUD.tscn`（PhaseHintLabel）
- `scripts/ui/shop.gd` + `scenes/ui/Shop.tscn`（BuildRow + BuildBaseButton）
- `scenes/Cell.tscn`（BuildingSprite 子节点）

**测试点**：
1. 启动 → 显示"请放置第一个基地" → 点任意格 → 基地放置 + 周围 3×3 预开
2. 安全区内无雷，雷分布在安全区外
3. 后续基地只能在已开格上建，价格 80/160/320 递增
4. 放完基地后 game_phase=playing，opener/marker 可正常购买作业

### P7：检测型 + 矿脉

**改动文件**：
- `scripts/robots/detector_robot.gd`（新）
- `scenes/DetectorRobot.tscn`（新）
- `scripts/cell.gd`（is_vein/vein_resources/become_vein/deplete_vein）
- `scripts/grid.gd`（get_all_flagged_cells + vein_created 信号）
- `scripts/robot_manager.gd`（spawn_robot 分支 + remove_robot + robot_removed 信号）
- `scripts/autoload/game_state.gd`（detector_count/detector_speed_level）
- `scripts/main.gd`（_on_robot_removed → hud.show_toast）
- `scripts/ui/hud.gd` + `scenes/ui/HUD.tscn`（ToastLabel + show_toast）
- `scripts/ui/shop.gd` + `scenes/ui/Shop.tscn`（BuyDetectorButton）
- `scenes/Cell.tscn`（VeinSprite 子节点）

**测试点**：
1. 标几个旗子 → 买检测型 → 自动走到旗子旁 → 3 秒检测
2. 旗子确实是雷 → +10 分 → 旗子变金色矿脉（100 资源）
3. 旗子标错 → 检测型自爆消失 → HUD 显示 toast
4. 多个旗子 → 检测型自动找下一个

### P8：矿工

**改动文件**：
- `scripts/robots/miner_robot.gd`（新）
- `scenes/MinerRobot.tscn`（新）
- `scripts/robot.gd`（抽取 `_move_step` 公共方法）
- `scripts/robot_manager.gd`（spawn_robot 加 miner 分支）
- `scripts/grid.gd`（get_all_veins + vein_depleted 信号）
- `scripts/autoload/game_state.gd`（miner_count/miner_speed_level/cargo_changed 信号/get_nearest_base）
- `scripts/ui/shop.gd` + `scenes/ui/Shop.tscn`（BuyMinerButton）

**测试点**：
1. 有矿脉 → 买矿工 → 走到矿脉 → 每 2 秒 cargo +5
2. cargo=20 → 自动回最近基地 → 卸货（钱+20 分+20）→ cargo=0 → 再找矿脉
3. 矿脉耗尽（resources=0）→ 变普通已开格 → 矿工找下一个
4. 多矿工同采一个矿脉 → 加速耗尽
5. 没有矿脉时矿工原地待命

### P9：充能塔

**改动文件**：
- `scripts/cell.gd`（is_charge_tower/become_charge_tower）
- `scripts/grid.gd`（place_charge_tower）
- `scripts/robot.gd`（buff_until/speed_mult/apply_buff/accumulate 修改）
- `scripts/robot_manager.gd`（tick_charge_towers + _tower_cooldowns + _find_robots_in_range）
- `scripts/autoload/game_state.gd`（charge_towers/charge_tower_count）
- `scripts/main.gd`（placing_mode 加 charge_tower）
- `scripts/ui/shop.gd` + `scenes/ui/Shop.tscn`（BuildTowerButton）

**测试点**：
1. 建充能塔 → 机器人进入 5×5 范围 → 每 5 秒被充能 → 速度 +50% 持续 5 秒
2. 范围外机器人不受影响
3. buff 与升级乘法叠加（Lv2 升级 1.0s + buff 1.5x = 0.67s/tick）

### P10：无人机

**改动文件**：
- `scripts/grid.gd`（get_farthest_closed_cells）
- `scripts/main.gd`（_trigger_drone）
- `scripts/ui/shop.gd` + `scenes/ui/Shop.tscn`（DroneButton）

**测试点**：
1. 点无人机按钮 → 钱 -100 → 3 个最远关闭格被打开
2. 打开的格子不给钱/分（by_actor="drone"）
3. 触发连锁展开规则

### P11：局外成长

**改动文件**：
- `scripts/autoload/save_system.gd`（新）
- `scripts/autoload/game_state.gd`（reset_state 读 SaveSystem）
- `scripts/ui/upgrade_panel.gd` + `scenes/ui/UpgradePanel.tscn`（局外升级行）
- `scripts/ui/results_panel.gd`（结算转矿石）
- `scripts/ui/hud.gd` + `scenes/ui/HUD.tscn`（OreLabel）
- `project.godot`（注册 SaveSystem autoload）

**测试点**：
1. 玩一局 → 结算 → 得到 score/10 矿石 → 重开 → 矿石保留
2. 升级面板花矿石解锁 detector → 重开 → Shop 中 detector 按钮可点
3. 升级"起始金币" → 重开 → 起始钱增加
4. 删除 `user://save_data.json` → 重开 → 回到默认值

---

## 5. 风险与权衡

| 风险 | 影响 | 缓解 |
|---|---|---|
| 4 种机器人 + 多充能塔 tick 开销 | 性能 | 16×16=256 格，单次 BFS 微秒级；实测 20 机器人 < 1ms/帧，无需优化 |
| `get_all_flagged_cells / get_all_veins` 每次扫描全图 | 性能 | 256 格遍历 < 0.1ms；不做索引缓存，profile 后再决定 |
| detector 自爆时 `queue_free` 延迟 | 同帧访问已释放对象 | `remove_robot` 先 `robots.erase` 再 `queue_free`，调用方同一帧内不再访问 |
| 第一个基地点在边界 | 安全区超出地图 | `safe_coords` 用 `cells.has(sc)` 过滤 |
| 旧存档缺少新字段 | 解锁状态丢失 | load_game 逐字段合并，新字段用默认值 |
| 充能塔 buff 与升级冲突 | 速度计算错误 | 乘法叠加（base / mult），buff_until 用时间戳到期自动失效 |
| detector 锁定旗子期间旗子被取消 | 锁残留 | do_tick 起始处清理失效目标 |
| `get_node("/root/Main/...")` 硬编码路径 | 重构脆弱 | v0.1 已有此风格，v0.2 保持一致；未来可改用信号总线 |

---

## 6. 验证方式（端到端）

按 CLAUDE.md 指令启动游戏：

```bash
"E:\其他\chorme_download\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64_console.exe" --path "f:\godot_game\扫雷_增量\saolei"
```

每个阶段完成后**手动验证**：

| 阶段 | 关键路径 |
|---|---|
| P6 | 启动 → 放基地 → 雷正确生成 → 后续 opener/marker 正常工作 |
| P7 | 标旗 → 买 detector → 走过去检测 → 变矿脉 / 自爆 toast |
| P8 | 有矿脉 → 买 miner → 采集 → 回基地卸货 → 钱分增长 |
| P9 | 建充能塔 → 机器人加速可见 |
| P10 | 点无人机 → 远处格子被开 |
| P11 | 结算 → 矿石增加 → 重开保留 → 升级面板可用 |

**回归测试**：每个阶段后跑一局完整 90 秒，确认前面阶段的功能未受影响。

**控制台监控**：用 console 版 Godot 可见 push_warning / push_error 输出，留意存档读写、节点释放相关警告。

---

## 7. 文件清单

### 新增（5 个）
- `scripts/robots/detector_robot.gd`
- `scripts/robots/miner_robot.gd`
- `scripts/autoload/save_system.gd`
- `scenes/DetectorRobot.tscn`
- `scenes/MinerRobot.tscn`

### 修改（13 个）
- `scripts/autoload/game_state.gd`（大改）
- `scripts/grid.gd`（大改）
- `scripts/cell.gd`（中改）
- `scripts/robot.gd`（小改，抽 `_move_step`）
- `scripts/robot_manager.gd`（中改）
- `scripts/main.gd`（中改）
- `scripts/map_generator.gd`（小改，加 generate_excluding）
- `scripts/ui/shop.gd`（中改）
- `scripts/ui/upgrade_panel.gd`（大改）
- `scripts/ui/hud.gd`（小改）
- `scripts/ui/results_panel.gd`（小改）
- `scenes/Cell.tscn` / `scenes/ui/Shop.tscn` / `scenes/ui/HUD.tscn` / `scenes/ui/UpgradePanel.tscn`（中改）
- `project.godot`（小改，加 SaveSystem autoload）

> solver.gd / pathfinding.gd / map_generator.gd（除新增方法外）保持不变。
