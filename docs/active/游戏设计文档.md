# 扫雷挖矿 · 游戏设计文档

> **版本**：v0.2（扩展设计阶段）
> **最后更新**：2026-08-03
> **项目目录**：`f:\godot_game\扫雷_增量\saolei`

---

## 1. 概述

### 1.1 核心概念
将经典扫雷与自动化经营 + 产业链管理结合的 **90 秒积分制** 小游戏。玩家在扫雷地图上挖矿、标雷、检测、运输，部署 4 种机器人协同作业，构建"手动操作 + 自动化"的产业循环。

**一句话总结**：扫雷的规则、挖矿的味道、4 种机器人分工协作、矿脉产业链、90 秒冲分。

### 1.2 玩家体验目标
- **前期（0-20s）**：纯扫雷 + 建基地 + 第一个机器人，玩家手点攒钱
- **中期（20-50s）**：检测型 + 矿工进场，矿脉产业链开始运转
- **后期（50-90s）**：多机器人协同、充能塔加成，玩家专注做模糊推理和路径规划
- **整体曲线**：从手动到自动，从单线到多线，节奏越来越快

### 1.3 平台
- 引擎：Godot 4.6
- 视角：2D 俯视格子地图
- 操作：鼠标

---

## 2. 核心玩法

### 2.1 游戏目标
在 90 秒内尽可能获得高积分。提前清完所有安全格 = 胜利结算。

### 2.2 核心循环

```
玩家点击格子（开格 / 标雷）
   ↓
获得钱 + 积分
   ↓
攒钱建基地 + 买机器人
   ↓
检测型验证旗子 → 生成矿脉
   ↓
矿工采集 → 送回基地 → 得分得钱（被动收入）
   ↓
充能塔加速、无人机开图
   ↓
产业链雪球
   ↓
时间到 → 结算积分 → 转化为矿石（局外货币）
```

### 2.3 胜利条件
- **提前清完**：所有非雷格子被打开 → 立即胜利结算
- **撑到时间到**：90 秒结束时按当前积分结算

### 2.4 失败 / 结束条件
- **命用完**：3 条命归零 → 立即失败结算（保留已得积分）
- **时间到未清完**：按当前积分结算（不算失败，但分数受限）

---

## 3. 地图与基础机制

### 3.1 地图规格

| 项 | 值 |
|---|---|
| 网格大小 | 16 × 16 = 256 格 |
| 地雷数 | 32 个（密度 ~12.5%） |
| 安全格数 | 224 个 |
| 预开区域 | 由玩家放置基地时确定（基地 + 周围 3×3） |

### 3.2 格子类型

| 类型 | 描述 | 视觉 |
|---|---|---|
| 关闭（墙） | 未挖掘的墙，未知状态 | 深色方块（岩石感） |
| 打开 - 空 | 周围 8 格无雷 | 浅色底（泥土感） |
| 打开 - 数字 | 周围 8 格有 N 个雷（1-8） | 数字（按经典扫雷配色） |
| 标记（旗） | 玩家或机器人标记为雷 | 旗子图标 |
| **矿脉** | 检测型验证正确的旗子变化而来 | 金色发光脉动 |
| 坍塌 | 玩家踩雷后的格子 | 暗红 / 裂纹（不能再操作） |
| **基地** | 玩家建造的卸货点 | 蓝色建筑图标，1 格 |
| **充能塔** | 玩家建造的加速建筑 | 紫色塔图标，1 格 |

### 3.3 数字逻辑
打开的格子显示其周围 8 个邻格中地雷的数量（0-8）。**0 时显示为空白，并自动连锁打开所有相连的 0 格子**（标准扫雷规则）。

### 3.4 预开区域（机制变更）
- 关卡开始时**全图关闭**
- 玩家必须**手动放置第一个基地**（进入放置模式后点击任意位置）
- 基地放置后，**基地所在格 + 周围 3×3 自动预开**（共 9 格）
- 地雷生成时避开预开区域（保证开局不立刻踩雷）

### 3.5 地雷生成规则
1. 初始化 16×16 全关闭
2. 玩家放置基地 → 标记基地周围 3×3 为"预开区域"
3. 在剩余格子中随机选 32 个放雷
4. 计算每个非雷格子的相邻雷数
5. 打开预开区域的 9 格（应用连锁展开规则）

### 3.6 矿脉

| 项 | 值 |
|---|---|
| 生成 | 检测型机器人验证"已被标记的旗子"成功 → 旗子格变为矿脉 |
| 资源量 | 单格 100 分 |
| 视觉 | 金色发光脉动 |
| 通行 | 不阻挡，机器人可踩过 |
| 采集 | 被矿工采完后变为普通已开格 |

---

## 4. 玩家操作

### 4.1 左键：开格子
- 点关闭格子 → 打开
- 如果是雷 → 扣 1 命，该格子变为"坍塌"状态
- 如果是数字 / 空 → 正常显示，空格连锁展开

### 4.2 右键：标雷
- 点关闭格子 → 切换标记 / 取消标记
- **标对一个雷** → +5 钱 +5 分
- **标错一个雷**（实际不是雷）→ -3 分（不扣钱）
- 已打开格子不能标记

### 4.3 双击：和弦（Chord）
- 双击数字格子
- 若该格子周围**所有雷都已被正确标记**，自动打开剩余的所有安全邻格
- 若标记数 ≠ 数字 或标错，无效果

### 4.4 建造操作（新）

#### 建基地
- 商店点击"建基地" → 进入放置模式
- 鼠标变光标，在**已开格子上**点击 → 建造
- 不可在矿脉 / 旗子 / 雷 / 已有建筑上建
- 钱不够时按钮变灰
- ESC 或右键取消

#### 建充能塔
- 同上流程

### 4.5 无人机技能（新）
- 商店点击"无人机" → **立即触发**（无需选位置）
- 自动打开地图上 3 个最远的关闭格
- 不算玩家操作，不给奖励
- 触发连锁展开规则

### 4.6 操作限制
- 一格一机 / 一格一建筑
- 玩家可以正常开 / 标格子（即使有建筑或机器人在格上）

---

## 5. 经济系统

### 5.1 三货币系统

| 货币 | 用途 | 来源 | 关卡间 |
|---|---|---|---|
| **钱** | 关卡内消耗（建基地、买机器人、买塔、用技能） | 玩家行动、机器人行动 | 不保留 |
| **积分** | 纯成绩，不可消耗 | 玩家行动、机器人行动 | 最终成绩 |
| **矿石**（新） | 局外升级解锁 | 积分转化（÷10） | 永久保留 |

**关键设计**：钱和积分完全独立。**花钱买机器人不会降低积分**。

### 5.2 起始资源

| 项 | 起始值 |
|---|---|
| 钱 | 100 |
| 积分 | 0 |
| 命 | 3 |
| 时间 | 90 秒 |
| 机器人 | 0 |
| 基地 | 0（玩家需手动建第一个） |

### 5.3 行动奖励表

| 行动 | 钱 | 积分 | 备注 |
|---|---|---|---|
| 玩家开一格（安全） | +1 | +1 | |
| 玩家标对一个雷 | +5 | +5 | "挖矿"高回报 |
| 玩家标错一个雷 | 0 | -3 | 不扣钱，避免雪上加霜 |
| 玩家踩雷 | 0 | 0 | -1 命 |
| opener 开一格 | +1 | +1 | 被动收入 |
| marker 标对一个雷 | +5 | +5 | 被动收入 |
| **detector 验证成功** | 0 | **+10** | 纯分不给钱（避免套娃） |
| **detector 验证失败** | 0 | 0 | 自爆消失，需重买 |
| **miner 卸货（每 1 分矿）** | **+1** | **+1** | 矿脉 100 分 = 100 钱 + 100 分 |

### 5.4 踩雷后果
- 扣 1 命
- 该格子变为"坍塌"状态（视觉上不同，不能再开 / 标，但视为已打开）
- 不影响其他格子，游戏继续

---

## 6. 机器人系统

### 6.1 机器人类型（4 种）

| 类型 | 颜色 | 图标 | 行为 | 基础价格 |
|---|---|---|---|---|
| **开墙型**（opener） | 蓝色 | ⛏ | 打开确定安全的墙体 | 50 |
| **标雷型**（marker） | 黄色 | ⚑ | 标记确定是雷的墙体 | 50 |
| **检测型**（detector） | 紫色 | 🔍 | 验证旗子，成功则旗子变矿脉 | 80 |
| **矿工型**（miner） | 橙色 | ⛏ | 走到矿脉采集，装满后回基地卸货 | 60 |

**严格类型分工**：每种机器人只做自己的事，不会跨界作业。

### 6.2 行为规则

#### opener / marker（与 v0.1 一致）
- 基于**扫雷推理的两条规则**，只执行 100% 确定的操作：
  - **规则 1（可开）**：数字格已标雷数 = 数字 → 其余未开邻格都安全
  - **规则 2（可标）**：数字格未开邻格数 = 数字 → 这些邻格全是雷
- 基础速度 2.0 秒 / 次

#### detector（新）

**判定逻辑**：
- 找到地图上所有"已被标记的旗子"（玩家手标 或 marker 标的都算）
- 走到旗子 8 邻接位置
- **检测 3 秒**（基础速度）
- **成功（旗子确实是雷）**：
  - +10 积分
  - 旗子格变为矿脉（100 分资源）
  - 旗子标记状态清除
- **失败（旗子不是雷）**：
  - 检测型自爆消失（机器人真的没了）
  - 旗子标记保留（玩家可手动右键取消错误标记）

**寻路**：与 opener 相同，BFS 在已开区域

#### miner（新）

**容量制运货**：

| 项 | 值 |
|---|---|
| 容量 | 20 分矿物 |
| 采集速度 | 5 分 / 2 秒 |
| 移动速度 | 与其他机器人一致 |
| 卸货 | 瞬间结算，+cargo 钱 +cargo 分 |

**工作流（状态机）**：

```
找最近矿脉 → 走过去（to_mine）
   ↓
采集（mining）→ 每次 +5 分到 cargo
   ↓
cargo 满 20 分？
   是 → 找最近基地 → 走回去（to_base）
        ↓
        卸货（unloading）→ 瞬间得 +20 钱 +20 分 → cargo 清零
   否 → 继续采
   ↓
矿脉耗尽（100 分被采完）→ 矿脉格变普通已开格 → 找下一个
```

**多矿工同采**：允许，加速耗尽速度（每个矿工的 cargo 独立计算）

**寻路优先级**：
1. 身上有 cargo → 找最近基地
2. 身上无 cargo → 找最近矿脉

### 6.3 寻路与协调

#### 寻路
- 机器人**只能在已开格子上行走**（关闭的墙是障碍）
- 矿脉 / 基地 / 充能塔 → 视为可通行
- 使用 **BFS** 在已开区域找最近目标

#### 多机协调
- 系统维护全局"已锁定目标"集合
- 机器人扫描目标时，跳过已被其他机器人锁定的目标
- 矿工同采一个矿脉时**不互相阻塞**（但 cargo 独立）

#### 空闲行为
- 若无可执行操作 → 原地待命
- 当玩家或机器人开新格子产生新数字 → 触发重新扫描

### 6.4 机器人购买（成本递增）

| 第 N 个 | opener / marker | detector | miner |
|---|---|---|---|
| 1 | 50 | 80 | 60 |
| 2 | 100 | 160 | 120 |
| 3 | 200 | 320 | 240 |
| 4 | 400 | 640 | 480 |
| ... | base × 2^(N-1) | 同 | 同 |

折扣升级适用所有机器人类型。

### 6.5 升级系统

**全局升级**：升级一次，所有同类机器人都受益。

| 升级项 | Lv 0 | Lv 1 | Lv 2 | 价格 |
|---|---|---|---|---|
| opener 速度 | 2.0s | 1.5s | 1.0s | 100 / 200 |
| marker 速度 | 2.0s | 1.5s | 1.0s | 100 / 200 |
| detector 速度 | 3.0s | 2.5s | 2.0s | 150 / 300 |
| miner 速度 | 2.0s | 1.5s | 1.0s | 150 / 300 |
| 购买折扣 | 0% | -25% | -50% | 200 / 500 |

---

## 7. 建筑系统（新）

### 7.1 基地

| 项 | 值 |
|---|---|
| 价格 | 第 1 个 80，第 2 个 160，第 3 个 320 ...（base × 2^(N-1)） |
| 占格 | 1 格，不可被炸毁 |
| 位置限制 | 只能在已开格子上建，不可在矿脉 / 旗子 / 雷 / 已有建筑上建 |
| 功能 | 矿工卸货点 + 新机器人出生点 |
| 数量 | 不限（价格递增为软上限） |

**设计意图**：玩家后期可建前哨基地，缩短矿工运输路径，让"基地放哪"成为策略决策。

### 7.2 充能塔

| 项 | 值 |
|---|---|
| 价格 | 300 |
| 占格 | 1 格，不重叠 |
| 范围 | 5×5（自身 + 周围两圈，共 25 格） |
| 效果 | 每 5 秒充能范围内 1 个机器人 → 5 秒内工作 / 移动速度 +50% |
| 充能对象 | 范围内任意类型机器人（含 detector / miner） |
| CD | 5 秒（覆盖率约 50%） |

**设计意图**：让"机器人 / 建筑放哪"成为真正的策略决策，鼓励玩家集中布局。

---

## 8. 技能系统（新）

### 8.1 无人机

| 项 | 值 |
|---|---|
| 价格 | 100 |
| 触发 | 商店点击立即生效，无需选位置 |
| 效果 | 打开地图上 3 个最远的关闭格 |
| 奖励 | 不给钱 / 分（但触发连锁展开） |
| 数量 | 一次性消耗品 |

**用途**：机器人卡死时破局、快速扩大视野、触发连锁打开大片区域。

---

## 9. 关卡结构

### 9.1 当前版本范围
- 单关原型（90 秒）
- 多关、跨关继承通过**局外成长系统**体现（见第 10 章）

### 9.2 关卡参数

| 项 | 值 |
|---|---|
| 时长 | 90 秒 |
| 命数 | 3（每关独立，不跨关） |
| 起始钱 | 100 |
| 起始机器人 | 0 |
| 起始基地 | 0（玩家手动建第一个） |

---

## 10. 局外成长系统（新）

### 10.1 货币：矿石
- 每局结算时按**积分 ÷ 10**（向下取整）转化获得
- 例：一局得 350 分 → 获得 35 矿石
- 永久保留，跨局累积
- 失败局也给矿石（保留激励）

### 10.2 升级树

| 升级项 | 等级 | 效果 | 累计矿石 |
|---|---|---|---|
| 起始金币 | 1-5 | +10/级（100→110→...→150） | 5 / 12 / 25 / 45 / 75 |
| 起始命数 | 1-3 | +1/级（3→4→5→6） | 10 / 30 / 60 |
| 机器人通用速度 | 1-3 | 基础间隔 -10%/级 | 15 / 35 / 70 |
| 初始预开区域 | 1-3 | 基地周围 3×3 → 5×5 → 7×7 | 10 / 30 / 60 |
| 起始送机器人 | 1-2 | 开局送 1 opener / 再送 1 marker | 20 / 50 |
| **detector 解锁** | — | 解锁购买 | 30 |
| **miner 解锁** | — | 解锁购买 | 30 |
| **充能塔解锁** | — | 解锁建造 | 50 |
| **无人机解锁** | — | 解锁使用 | 40 |

### 10.3 首局体验
- 玩家 0 矿石开局，默认**只能买 opener + marker**
- 通过 1-2 局积累矿石 → 解锁 detector → 矿脉产业链激活
- 再积累 → 解锁 miner / 充能塔 / 无人机 → 完整 build
- 系统逐步开放，避免新手一上来面对 4 种机器人 + 2 种建筑懵逼

---

## 11. UI/UX 设计

### 11.1 HUD 布局（建议）

```
+----------------------------------------------------+
|  钱: 150   积分: 230   命: 3/3   时间: 0:42       |
|                            矿石: 35（局外）        |
+----------------------------------------------------+
|                                                    |
|                                                    |
|              [16 × 16 游戏地图]                    |
|                                                    |
|                                                    |
|                                                    |
+----------------------------------------------------+
| [opener¥50]  [marker¥50]  [detector¥80]  [miner¥60]|
| [建基地¥80]  [建塔¥300]   [无人机¥100]  [升级面板] |
+----------------------------------------------------+
```

### 11.2 商店 UI
- 底部按钮栏（两行）
- 第一行：4 种机器人
- 第二行：建筑、技能、升级
- **未解锁的按钮显示**"🔒 需 X 矿石"，不可点击
- 钱不够时按钮变灰

### 11.3 升级面板
- 弹出式面板（点击"升级面板"按钮触发）
- 列出所有可升级项
- 显示当前等级、下一级效果、价格
- 不可升级时显示"已满级"

### 11.4 视觉风格
- **整体**：像素风或简洁扁平风（待定）
- **已开区域**：暖色（土黄 / 棕色，泥土感）
- **关闭墙体**：深色（深棕 / 灰，岩石感）
- **数字**：经典扫雷配色（1 蓝、2 绿、3 红、4 深紫...）
- **机器人**：醒目色彩，移动有平滑动画，作业时有小动画
- **矿脉**：金色脉动光效
- **基地**：蓝色建筑图标
- **充能塔**：紫色塔图标，充能时有光束特效
- **卸货动画**：矿工在基地短暂闪光，分数飞出

---

## 12. 实现要点

### 12.1 扫雷求解器（opener / marker 核心）
输入当前地图状态，输出所有 100% 确定的操作。

```python
def find_certain_actions(board):
    actions = []
    for cell in board.opened_cells:
        if cell.number == 0:
            continue
        neighbors = board.get_neighbors(cell)
        flagged = [n for n in neighbors if n.is_flagged]
        unopened = [n for n in neighbors if n.is_unopened]

        # 规则 1：所有雷已标 → 其余可开
        if len(flagged) == cell.number:
            for n in unopened:
                actions.append(("open", n))

        # 规则 2：未开邻格数 = 剩余雷数 → 全是雷
        if len(unopened) + len(flagged) == cell.number:
            for n in unopened:
                actions.append(("flag", n))
    return actions
```

### 12.2 矿工状态机（新）

```python
class Miner:
    state: str  # "to_mine" | "mining" | "to_base" | "unloading"
    cargo: int = 0
    capacity: int = 20
    current_vein: Cell = None

    def tick():
        match state:
            "to_mine":
                if current_vein is None or current_vein.empty:
                    current_vein = find_nearest_vein()
                if current_vein:
                    move_toward(current_vein)
                    if adjacent_to(current_vein):
                        state = "mining"
            "mining":
                if cargo >= capacity:
                    state = "to_base"
                elif current_vein.empty:
                    current_vein = None
                    state = "to_mine"
                else:
                    mine_5_points()  # 每 2 秒
                    cargo += 5
                    current_vein.resources -= 5
            "to_base":
                base = find_nearest_base()
                move_toward(base)
                if adjacent_to(base):
                    state = "unloading"
            "unloading":
                gain(cargo)  # +cargo 钱 +cargo 分
                cargo = 0
                state = "to_mine"
```

### 12.3 检测型机器人（新）

```python
class Detector:
    def find_target():
        # 找所有"已标记但未变矿脉"的旗子
        return [c for c in cells if c.is_flagged and not c.is_vein]

    def detect(flag_cell):
        wait(3 seconds)  # 检测中
        if flag_cell.is_mine:
            score += 10
            flag_cell.become_vein(resources=100)
        else:
            self.die()  # 自爆，机器人消失
```

### 12.4 充能塔（新）

```python
class ChargeTower:
    range: int = 2  # 5×5
    cooldown: float = 0.0
    COOLDOWN_MAX: float = 5.0

    def tick(delta):
        if cooldown > 0:
            cooldown -= delta
            return
        robots_in_range = find_robots_within(range)
        if robots_in_range:
            target = pick_one(robots_in_range)
            target.apply_buff(duration=5.0, speed_mult=1.5)
            cooldown = COOLDOWN_MAX
```

### 12.5 无人机（新）

```python
def trigger_drone(grid):
    closed_cells = grid.get_all_closed_cells()
    if len(closed_cells) <= 3:
        targets = closed_cells
    else:
        # 取离地图中心最远的 3 个
        targets = sorted(closed_cells, key=lambda c: -distance(c, center))[:3]
    for cell in targets:
        grid.open_cell(cell, by_actor="drone", give_reward=False)
```

### 12.6 寻路实现
BFS 在已开区域找最近目标。矿工寻路时优先级：
1. cargo > 0 → 找最近基地
2. cargo = 0 → 找最近矿脉

```python
def find_nearest_target(grid, start, targets, locked, robot_at, except_robot):
    queue = [(start, [])]
    visited = {start}
    while queue:
        pos, path = queue.pop(0)
        for t in targets:
            if is_adjacent_8(pos, t) and not is_locked(t, locked, except_robot):
                return (t, path, pos)
        for n in opened_neighbors(pos):
            if n not in visited and not blocked_by_other_robot(n):
                visited.add(n)
                queue.append((n, path + [n]))
    return None
```

### 12.7 主循环（每帧）

```
1. 处理玩家输入（点击、建造、技能）
2. 更新计时器（每秒 -1）
3. 触发机器人 tick（按各机器人的速度间隔）
4. 触发建筑 tick（充能塔充能 CD）
5. 检查胜利 / 失败条件
6. 渲染
```

### 12.8 数据结构（更新）

```python
class Cell:
    x: int
    y: int
    is_mine: bool
    is_opened: bool
    is_flagged: bool
    is_collapsed: bool
    number: int          # 0-8
    is_vein: bool        # 是否矿脉
    vein_resources: int  # 矿脉剩余资源（0-100）
    is_base: bool        # 是否基地
    is_charge_tower: bool

class Robot:
    x: int
    y: int
    type: RobotType      # OPENER / MARKER / DETECTOR / MINER
    last_tick_time: float
    current_target: Cell
    cargo: int = 0       # 仅 MINER 使用
    state: str           # 状态机（仅 MINER 需要）
    buff_until: float    # 充能塔加成到期时间

class ChargeTower:
    coord: Vector2i
    cooldown: float

class GameState:
    bases: Array         # 所有基地坐标
    charge_towers: Array # 所有充能塔
    ore: int             # 局外矿石（永久）
    unlocks: Dictionary  # 各项解锁状态
```

---

## 13. 完整数值表（速查）

| 项 | 值 |
|---|---|
| 地图大小 | 16 × 16 |
| 地雷数 | 32（密度 12.5%） |
| 关卡时长 | 90 秒 |
| 起始命 | 3 |
| 起始钱 | 100 |
| 玩家开格 | +1 钱 +1 分 |
| 玩家标对雷 | +5 钱 +5 分 |
| 玩家标错雷 | -3 分（不扣钱） |
| 玩家踩雷 | -1 命 |
| opener 开格 | +1 钱 +1 分 |
| marker 标对雷 | +5 钱 +5 分 |
| **detector 验证成功** | **+10 分** |
| **detector 验证失败** | **自爆消失** |
| **miner 卸货（每分矿）** | **+1 钱 +1 分** |
| **矿脉资源量** | **100 分 / 格** |
| **矿工容量** | **20 分** |
| **矿工采集速度** | **5 分 / 2 秒** |
| 充能塔范围 | 5×5 |
| 充能塔效果 | +50% 速度 5 秒，CD 5 秒 |
| 无人机 | 打开 3 个远处格子 |
| opener / marker 价格 | 50 / 100 / 200 / 400 ... |
| detector 价格 | 80 / 160 / 320 ... |
| miner 价格 | 60 / 120 / 240 ... |
| 基地价格 | 80 / 160 / 320 ... |
| 充能塔价格 | 300 |
| 无人机价格 | 100 |
| opener / marker 速度升级 | 2.0s → 1.5s → 1.0s |
| detector 速度升级 | 3.0s → 2.5s → 2.0s |
| miner 速度升级 | 2.0s → 1.5s → 1.0s |
| 购买折扣升级 | 0% → 25% → 50% |
| **矿石转化比** | **积分 ÷ 10** |

---

## 14. 未来扩展方向（暂不实现）

- **多关闯关**：每关地图变大、雷密度增加、特殊地块
- **特殊地块**：无法开的硬墙、价值更高的稀有雷、爆炸雷（连锁伤害）
- **第 5 种机器人**：探测型（揭示未知区域属性）、加成型（buff 站位）
- **挑战模式**：每日关卡、不踩雷通关、极限速通
- **基地升级**：基地可花钱升级，扩大预开范围 / 加速矿工卸货

---

## 15. 已知设计风险

### 15.1 雪球失控
机器人 + 矿脉产业链可能让积分指数级增长。
- **缓解**：价格翻倍递增、矿工运输时间天然限制
- **待实测调整**

### 15.2 视觉拥挤
4 种机器人 + 矿脉 + 建筑同屏，可能干扰玩家阅读局面。
- **缓解**：机器人半透明、矿脉发光但不喧宾夺主、提供关闭显示选项

### 15.3 检测型失败挫败感
80 块买的检测型自爆，玩家可能 rage quit。
- **缓解**：失败时给清晰反馈"你标错了"，引导玩家学习
- **替代方案**：失败扣分（-20）不死亡

### 15.4 矿工路径死锁
矿工找不到基地（被雷围困）时不知所措。
- **缓解**：UI 提示"矿工无法返回基地"，引导玩家开路

### 15.5 学习曲线陡峭
4 种机器人 + 2 种建筑 + 1 个技能 + 局外升级，新手懵。
- **缓解**：局外解锁机制让系统逐步开放
- **首局**：只有 opener + marker，简单上手

### 15.6 基地选址压力
玩家不知道基地放哪最优，可能选择困难。
- **缓解**：商店按钮提示"建议放在矿脉附近 / 中心区域"
- **预开区域**：基地周围自动预开，给玩家即时反馈

---

## 16. 实现阶段（建议顺序）

| 阶段 | 内容 | 优先级 |
|---|---|---|
| **P1-P5** | v0.1 已实现内容（扫雷核心、状态结算、商店、opener/marker、升级） | ✓ 已完成 |
| **P6** | **基地系统**（玩家建造、占格、矿工出生点） | 必须 |
| **P7** | **检测型机器人 + 矿脉系统**（验证旗子、生成矿脉、自爆逻辑） | 必须 |
| **P8** | **矿工型机器人**（容量制运货、状态机、卸货结算） | 必须 |
| **P9** | **充能塔**（建筑、范围充能、buff 系统） | 完整体验 |
| **P10** | **无人机技能**（一次性消耗、开远处格子） | 完整体验 |
| **P11** | **局外成长系统**（矿石、升级树、解锁、存档） | 完整体验 |
| **P12** | 视觉打磨（矿脉光效、充能光束、卸货动画） | 打磨版 |

**v0.2 MVP** = P6 + P7 + P8（核心产业链）
**v0.2 完整版** = P6 - P11

---

## 附录：术语表

| 术语 | 含义 |
|---|---|
| 墙 / 关闭格 | 未打开的格子，对机器人和玩家都是未知 |
| 开格子 | 把关闭格变为打开状态 |
| 标雷 | 在关闭格上放旗子，标记为雷 |
| 和弦 | 双击数字格，自动开完其周围所有安全邻格 |
| 坍塌 | 踩雷后的格子状态，不能再操作 |
| 开墙型（opener） | 蓝色 ⛏，专门打开确定安全的墙 |
| 标雷型（marker） | 黄色 ⚑，专门标记确定是雷的墙 |
| **检测型（detector）** | 紫色 🔍，验证旗子，成功生成矿脉，失败自爆 |
| **矿工型（miner）** | 橙色 ⛏，采集矿脉运回基地 |
| **矿脉（vein）** | 检测型验证旗子成功后产生的可采集资源点（100 分） |
| **基地（base）** | 玩家建造的卸货点 + 机器人出生点 |
| **充能塔（charge tower）** | 给范围内机器人加速的建筑 |
| **无人机（drone）** | 一次性技能，打开 3 个远处格子 |
| **矿石（ore）** | 局外货币，积分 ÷ 10 转化 |
| 求解器 | 计算所有 100% 确定操作的算法 |
| 雪球 | 资源指数级增长的失控现象 |
