# 扫雷挖矿 · 数值模型 v0.1

> **建模时间**：2026-08-06
> **目的**：用数学模型推算合适的单局时长、价格递增倍率、关卡目标分数、局外升级成本
> **配套文档**：`调研_同类游戏数值.md`（数据来源）、`游戏设计文档.md`（设计意图）
> **使用方式**：每个模型包含「参数 → 公式 → 代入示例 → 结论」四件套，可独立调整

---

## 0. 模型总览

5 个相互关联的子模型：

| 模型 | 解决的问题 | 输入 | 输出 |
|---|---|---|---|
| A. 单局产出 | 一局能赚多少分/钱 | 玩家行为 + 机器人配置 | 预期 score / money |
| B. 价格递增倍率 | 同类机器人/基地应该多贵 | 期望购买次数 + 预算 | multiplier m |
| C. 关卡目标分数 | 第 N 关要求多少分 | 玩家产出曲线 + 难度系数 | target(N) |
| D. 局外升级成本 | 解锁项该多少矿石 | 全解锁目标局数 | ore_cost(item, lv) |
| E. 失败容忍度 | 失败给多少矿石 | 留存目标 | fail_ore_ratio |

**核心约束**：模型 D 的输出（全解锁局数）必须与玩家实际产出（模型 A 累积）匹配，否则玩家要么解锁太快（饱和）、要么解锁太慢（流失）。

---

## 1. 模型 A：单局产出预测

### 1.1 参数

| 符号 | 含义 | 默认值 | 备注 |
|---|---|---|---|
| T | 单局时长（秒） | 180 | 微信小游戏甜点 |
| v_player | 玩家手点速度（操作/秒） | 0.6 | 含思考时间，新手 0.4 / 老手 0.9 |
| r_open | 开格占操作比 | 0.7 | 标雷占 0.3 |
| N_cells | 安全格数 | 224 | 16×16 - 32 雷 |
| N_mines | 地雷数 | 32 | |
| p_flag | 标雷正确率 | 0.8 | 玩家推理能力 |
| N_opener | opener 数量 | 1 | |
| N_marker | marker 数量 | 1 | |
| N_detector | detector 数量 | 1 | |
| N_miner | miner 数量 | 1 | |
| t_opener | opener 单次 tick（秒） | 1.5 | 建议（当前 2.0） |
| t_marker | marker 单次 tick（秒） | 1.5 | 建议（当前 2.0） |
| t_detector | detector 单次检测（秒） | 1.5 | 建议（当前 3.0） |
| t_miner_cycle | miner 单趟循环（秒） | 24 | 走+采+回+卸 |
| vein_value | 矿脉资源量 | 250 | 建议（当前 100） |
| cargo_capacity | miner 容量 | 25 | 建议（当前 20） |
| detector_money_reward | detector 验证成功给钱 | 15 | 建议（当前 0） |

### 1.2 公式

**玩家手点产出**：

```
N_open_hand = min(T × v_player × r_open, N_cells × 0.6)
N_flag_hand = min(T × v_player × (1 - r_open), N_mines × p_flag)

Score_hand = N_open_hand × 1 + N_flag_hand × 5
Money_hand = N_open_hand × 1 + N_flag_hand × 5
```

（N_cells × 0.6 是经验修正，玩家不可能扫完整张图）

**机器人产出**：

```
Score_opener = N_opener × (T / t_opener) × 1
Money_opener = same

Score_marker = N_marker × (T / t_marker) × 5
Money_marker = same

N_detect = min(N_detector × (T / t_detector), N_mines × p_flag)
Score_detector = N_detect × 10
Money_detector = N_detect × detector_money_reward

N_trips = N_miner × (T / t_miner_cycle)
Score_miner = N_trips × cargo_capacity × 1
Money_miner = N_trips × cargo_capacity × 1
```

**总产出**：

```
Score_total = Score_hand + Score_opener + Score_marker + Score_detector + Score_miner
Money_total = Money_hand + Money_opener + Money_marker + Money_detector + Money_miner
```

### 1.3 代入示例（默认参数）

T=180s, 1 个每种机器人：

```
玩家手点：
  N_open_hand = min(180 × 0.6 × 0.7, 224 × 0.6) = min(75.6, 134.4) = 76
  N_flag_hand = min(180 × 0.6 × 0.3, 32 × 0.8) = min(32.4, 25.6) = 25
  Score_hand = 76 + 25 × 5 = 201
  Money_hand = 201

机器人：
  Opener：180/1.5 × 1 = 120 → Score 120 / Money 120
  Marker：180/1.5 × 1 = 120 → Score 600 / Money 600
  Detector：min(120, 25) = 25 → Score 250 / Money 375
  Miner：180/24 × 1 = 7.5 → Score 188 / Money 188

总产出：
  Score ≈ 201 + 120 + 600 + 250 + 188 = 1359
  Money ≈ 1359
```

注意：marker 的产出（600）严重偏高，因为 marker 标对雷奖励 ×5。这是个**待修正的失衡点**。

### 1.4 修正参数（推荐）

| 项 | 默认 | 修正 | 理由 |
|---|---|---|---|
| 标雷奖励 | +5 钱 +5 分 | **+3 钱 +3 分** | marker 性价比是 opener 5×，过强 |
| t_marker | 1.5s | **2.0s** | 拉平与 opener |
| 玩家手点标雷 | +5 钱 +5 分 | **+5 钱 +5 分（保持）** | 玩家手点是高频正反馈，不能削弱 |

修正后重新算（T=180s, 1 个各机器人）：

```
玩家手点：Score 201 / Money 201（不变）
Opener：Score 120 / Money 120
Marker（2.0s, ×3 奖励）：180/2 × 3 = 270 → Score 270 / Money 270
Detector：Score 250 / Money 375
Miner：Score 188 / Money 188

总产出：Score ≈ 1029 / Money ≈ 1154
```

**结论**：单局典型产出约 **1000 分 / 1150 钱**（含产业链）。这是后续模型的基准值。

---

## 2. 模型 B：价格递增倍率

### 2.1 公式

```
cost(n) = base × m^(n-1)
累计 n 个总成本：S(n) = base × (m^n - 1) / (m - 1)
```

**反推倍率**（给定第 N 个的价格上限 C_max）：

```
m = (C_max / base)^(1/(N-1))
```

**反推倍率**（给定累计预算 B_N，购买 N 个）：

```
需要数值求解，近似公式（N 较大时）：
m ≈ 1 + base / (B_N / N)  （几何级数近似）
```

### 2.2 代入示例

**场景 1**：单局玩家可用机器人预算 400 钱，希望买 5 个机器人（含混合类型）

假设平均 base = 55（opener/marker 50 + detector/miner 80/60 加权）：

```
S(5) = 55 × (m^5 - 1) / (m - 1) = 400
试 m=1.3：55 × (3.71 - 1)/0.3 = 497（贵）
试 m=1.25：55 × (3.05 - 1)/0.25 = 451（接近）
试 m=1.2：55 × (2.49 - 1)/0.2 = 410（合适）
```

**结论**：m = 1.2 时玩家单局能买 5 个；m = 1.4 时只能买 4 个。

**场景 2**：每种机器人在整局游戏（多次会话累计）最多买 4 个

```
opener：base 50，期望第 4 个不超过 150
m = (150/50)^(1/3) = 1.442 ≈ 1.4

detector：base 80，期望第 4 个不超过 240
m = (240/80)^(1/3) = 1.442 ≈ 1.4

miner：base 60，期望第 4 个不超过 180
m = (180/60)^(1/3) = 1.442 ≈ 1.4
```

**结论**：所有机器人/建筑统一用 **m = 1.4**。

### 2.3 不同 m 值对比表

base = 50 时：

| n | m=1.2 | m=1.4 | m=1.6 | m=2.0（当前） |
|---|---|---|---|---|
| 1 | 50 | 50 | 50 | 50 |
| 2 | 60 | 70 | 80 | 100 |
| 3 | 72 | 98 | 128 | 200 |
| 4 | 86 | 137 | 205 | 400 |
| 5 | 104 | 192 | 328 | 800 |
| **累计 5** | **372** | **547** | **791** | **1550** |

**结论**：当前 ×2 递增下，玩家买 5 个机器人需要 1550 钱——单局根本买不起。×1.4 时只需 547，配合产业链回本能买齐 4-5 个。

---

## 3. 模型 C：关卡目标分数曲线

### 3.1 设计原则

关卡目标必须满足两个矛盾约束：

1. **可达成**：玩家升级后能达标（否则卡关流失）
2. **有挑战**：不能太容易（否则无升级动机）

数学表达：

```
target(N) = player_baseline(N) × difficulty(N)

其中：
player_baseline(N) = score_base × upgrade_factor(N)
upgrade_factor(N) ≈ 1.15^N  （每关玩家强度提升 15%）
difficulty(N) = 1.0 + 0.1 × log2(N)  （难度缓增）
```

### 3.2 简化公式（推荐）

```
target(N) = a + b × N + c × N^1.5
```

参数（基于模型 A 单局 ~1000 分基准）：

- a = 100（保底门槛）
- b = 50（线性增长）
- c = 15（亚指数增长）

### 3.3 代入示例

| N | target(N) | 玩家无升级产出 | 是否达标 |
|---|---|---|---|
| 1 | 165 | 1000 | ✓ 轻松 |
| 2 | 292 | 1000 | ✓ |
| 5 | 819 | 1000 | ✓ 边缘 |
| 6 | 1045 | 1000 | ✗ 需升级 |
| 10 | 2124 | 1000 | ✗ 需 2× 升级 |
| 15 | 3823 | 1000 | ✗ 需 4× 升级 |
| 20 | 5947 | 1000 | ✗ 需 6× 升级 |

**升级系数校准**：玩家从第 5 关到第 10 关，分数需要从 1000 提升到 2124，即 5 关内 ×2.12。每关需要强度提升 2.12^(1/5) = 1.16，约 **16%/关**。

### 3.4 升级项如何提供 16%/关 的强度

玩家可用的局外升级（参考设计文档 10.2）：

| 升级项 | 满级 | 累计强度提升 |
|---|---|---|
| 起始金币 +25/级 × 5 | +125 钱 | 间接（更多机器人）约 +15% |
| 通用速度 -8%/级 × 3 | interval ×0.78 | +28% |
| 起始送机器人 ×2 | +1 opener +1 marker | +20% |
| 地图扩展（更大空间） | +50% 格子 | +50% |
| detector/miner 解锁 | 启用产业链 | +50% |

如果玩家**均衡升级**，每关能获得约 **+15-20% 强度**，与 16%/关 匹配。

### 3.5 关卡阶段建议

| 关卡范围 | 设计意图 | target 增长率 |
|---|---|---|
| 1-5 | 引导期，无脑通关 | +50/关 |
| 6-15 | 成长期，开始需要升级 | +120/关 |
| 16-30 | 挑战期，必须均衡 build | +250/关 |
| 30+ | 极限期，全升级玩家才能通 | +500/关 |

---

## 4. 模型 D：局外升级成本

### 4.1 设计原则

调研共识：**前 10 局密集解锁，后稀疏，全解锁 30-50 局**。

转化为数学约束：

```
设解锁项总数 K = 9（参考设计文档 10.2）
设目标全解锁局数 N_total = 40

玩家累计矿石期望：
ore(n) ≈ n × avg_ore_per_game

avg_ore_per_game ≈ avg_score / 10 ≈ 100（按 1000 分/局）

约束：
- 第 1-10 局解锁 5 项（前 50%）
- 第 11-25 局解锁 3 项（中 33%）
- 第 26-40 局解锁 1 项（后 17%）
```

### 4.2 单项成本公式

```
cost(item, lv) = base_item × m_item^(lv - 1)
```

| item | base | m | max_lv | 累计成本 | 解锁时机（局） |
|---|---|---|---|---|---|
| 起始金币 | 5 | 1.6 | 5 | 5+8+13+20+32 = 78 | 1-3 |
| 起始命数 | 10 | 1.7 | 3 | 10+17+29 = 56 | 3-5 |
| 起始机器人 | 20 | 1.8 | 2 | 20+36 = 56 | 2-3 |
| **detector 解锁** | **30** | - | 1 | 30 | **2-3（必早）** |
| **miner 解锁** | **30** | - | 1 | 30 | **3-4（必早）** |
| 通用速度 | 15 | 1.7 | 3 | 15+25+43 = 83 | 5-10 |
| 地图扩展 | 20 | 1.8 | 3 | 20+36+65 = 121 | 8-15 |
| 充能塔解锁 | 50 | - | 1 | 50 | 10-15 |
| 无人机解锁 | 40 | - | 1 | 40 | 12-18 |

**累计全解锁**：78 + 56 + 56 + 30 + 30 + 83 + 121 + 50 + 40 = **544 矿石**

### 4.3 校验：玩家 40 局能否全解锁

```
玩家平均每局矿石 = 1000 分 / 10 = 100 矿石
40 局累计 = 4000 矿石（理论最大）
```

实际考虑：

- 玩家不会每局都拿 1000 分（前期 300-500，后期 1500-2000）
- 平均 800 矿石/局 是更现实的估算

修正：玩家 40 局 ≈ 800 × 40 / 10 = 3200 矿石

累计需求 544 远小于 3200，**说明解锁过快**。需要调高 base 或加更多层级。

### 4.4 修正方案

| item | base | m | max_lv | 累计 |
|---|---|---|---|---|
| 起始金币 | 10 | 1.5 | 5 | 10+15+22+34+51 = 132 |
| 起始命数 | 20 | 1.6 | 3 | 20+32+51 = 103 |
| 起始机器人 | 40 | 1.7 | 2 | 40+68 = 108 |
| detector 解锁 | 50 | - | 1 | 50 |
| miner 解锁 | 50 | - | 1 | 50 |
| 通用速度 | 30 | 1.6 | 3 | 30+48+77 = 155 |
| 地图扩展 | 50 | 1.7 | 3 | 50+85+145 = 280 |
| 充能塔解锁 | 100 | - | 1 | 100 |
| 无人机解锁 | 80 | - | 1 | 80 |

**累计全解锁**：1058 矿石

校验：

```
玩家平均 80 矿石/局 × 40 局 = 3200 矿石（仍超）
```

仍然超，但**留出了升级空间**（玩家可以多次升满级或加新升级项）。

### 4.5 失败容忍度对解锁的影响

如果失败局给 50% 矿石：

```
通关局：1000 分 / 10 = 100 矿石
失败局：500 分 / 10 = 50 矿石（保底）

玩家分布：
- 高手：80% 通关 + 20% 失败 = 90 矿石/局平均
- 新手：30% 通关 + 70% 失败 = 65 矿石/局平均
```

新手全解锁需要 1058 / 65 = **16 局**（仍较快）。

### 4.6 长线建议

如果希望全解锁需要 40+ 局（1-2 小时游戏时长），需要：

1. **加更多升级层级**（如起始金币 5 级 → 10 级）
2. **引入"声望"系统**（满级后重置获得倍率）
3. **加入"挑战关卡"**（高于主线，需要专精升级）

这是 v0.3+ 的问题，v0.1 先用当前模型。

---

## 5. 模型 E：失败容忍度

### 5.1 设计目标

- **不流失**：失败也有正反馈（给矿石）
- **有动力**：失败给的矿石明显少于通关（约 50-70%）
- **保底**：极差局也给最低反馈（避免挫败）

### 5.2 公式

```
ore_earned = max(
    floor(score_total / k),       # 标准转化
    floor(min_ore_per_game)       # 保底
)

其中：
k = 10（当前设计，可调）
min_ore_per_game = 10（保底）
```

**失败补偿**（建议加）：

```
if not passed_level:
    bonus = floor(target_score × 0.05)  # 失败给目标的 5%
    ore_earned += bonus
```

### 5.3 代入示例

| 场景 | score | 是否达标 | 标准 ore | 失败 bonus | 总 ore |
|---|---|---|---|---|---|
| 高手通关 | 1500 | ✓ | 150 | 0 | 150 |
| 普通通关 | 1000 | ✓ | 100 | 0 | 100 |
| 边缘失败 | 950 | ✗（目标 1045） | 95 | 52 | **147** |
| 早卡关 | 500 | ✗（目标 819） | 50 | 41 | **91** |
| 完全失败 | 100 | ✗ | 10 | 27 | **37** |

**关键洞察**：失败 bonus 让"差一点"的失败局反而比普通通关获得更多矿石（147 vs 100），这是**最强的复战刺激**——玩家会觉得"再试一次就过了"。

### 5.4 注意事项

失败 bonus 不要超过通关奖励太多，否则**失败比通关还赚**会导致玩家故意失败。建议：

```
fail_ore_max < pass_ore × 1.2
```

---

## 6. 综合数值落地表（v0.1 推荐）

基于以上 5 个模型，汇总推荐数值：

### 6.1 单局基础参数

| 项 | 当前 | 推荐v0.1 | 依据 |
|---|---|---|---|
| 单局时长 | 90s | **180s** | 模型 A + 调研 |
| 起始钱 | 100 | **150** | 模型 A 让玩家买 2 个机器人 |
| 起始命 | 3 | **3** | 不变 |
| 地图大小 | 16×16 | **16×16** | 不变 |
| 地雷数 | 32 | **32** | 不变 |

### 6.2 奖励数值

| 行动 | 当前 | 推荐v0.1 |
|---|---|---|
| 玩家开格 | +1 钱 +1 分 | 不变 |
| 玩家标对雷 | +5 钱 +5 分 | 不变 |
| 玩家标错雷 | -3 分 | 不变 |
| opener 开格 | +1 钱 +1 分 | 不变 |
| **marker 标对雷** | +5 钱 +5 分 | **+3 钱 +3 分** |
| **detector 验证成功** | +10 分 | **+10 分 +15 钱** |
| **detector 验证失败** | 自爆 | **-15 分，存活** |
| miner 卸货 | +1 钱 +1 分/分矿 | 不变 |

### 6.3 速度与容量

| 项 | 当前 | 推荐v0.1 |
|---|---|---|
| opener/marker tick | 2.0s | **1.5s** |
| detector 检测 | 3.0s | **1.5s** |
| miner 容量 | 20 | **25** |
| miner 采集速度 | 5/2s | **10/2s（每 tick +10）** |
| 矿脉资源量 | 100 | **250** |

### 6.4 价格递增

| 项 | 当前倍率 | 推荐倍率 |
|---|---|---|
| 所有机器人 | ×2 | **×1.4** |
| 基地 | ×2 | **×1.4** |

### 6.5 局外升级（首版）

| 升级项 | base | m | max_lv | 累计矿石 |
|---|---|---|---|---|
| 起始金币 | 10 | 1.5 | 5 | 132 |
| 起始命数 | 20 | 1.6 | 3 | 103 |
| 起始机器人 | 40 | 1.7 | 2 | 108 |
| detector 解锁 | 50 | - | 1 | 50 |
| miner 解锁 | 50 | - | 1 | 50 |
| 通用速度 | 30 | 1.6 | 3 | 155 |
| 地图扩展 | 50 | 1.7 | 3 | 280 |
| 充能塔解锁 | 100 | - | 1 | 100 |
| 无人机解锁 | 80 | - | 1 | 80 |
| **累计** | | | | **1058** |

### 6.6 矿石转化

| 项 | 当前 | 推荐v0.1 |
|---|---|---|
| 通关矿石转化 | score ÷ 10 | score ÷ 10 |
| 失败保底矿石 | (score ÷ 10) | **score ÷ 10 + target × 0.05** |
| 最低保底 | 0 | **10** |

---

## 7. 模型使用方法

### 7.1 验证单局平衡

```
1. 设定参数（T, v_player, 机器人数量）
2. 跑模型 A 计算 Score_total
3. 检查：Score_total 是否在关卡目标 target(N) 附近
4. 若 Score < target × 0.8 → 升级不足，玩家会卡关
5. 若 Score > target × 1.5 → 升级过头，玩家觉得太简单
```

### 7.2 验证解锁节奏

```
1. 假设玩家通关率 p（如 0.6）
2. 单局平均矿石 = p × score_pass / 10 + (1-p) × (score_fail / 10 + target × 0.05)
3. 累计 N 局后总矿石
4. 检查：能否在第 N 局解锁第 k 个升级项
5. 期望：前 10 局解锁 50%，40 局全解锁
```

### 7.3 玩家强度校准

```
player_strength(N) = base_score × Π(upgrade_factor_i)
target(N) / player_strength(N) 应在 [1.0, 1.3] 区间
```

---

## 8. 附录：GDScript 辅助函数

可粘贴到 `scripts/autoload/balance.gd`（建议新建）作为权威数值来源。

```gdscript
class_name Balance
extends Node

# ---- 单局参数 ----
const GAME_DURATION: float = 180.0
const STARTING_MONEY: int = 150
const STARTING_LIVES: int = 3

# ---- 奖励 ----
const REWARD_OPEN: int = 1
const REWARD_FLAG_PLAYER: int = 5
const REWARD_FLAG_MARKER: int = 3
const REWARD_DETECT_SCORE: int = 10
const REWARD_DETECT_MONEY: int = 15
const PENALTY_DETECT_FAIL: int = -15

# ---- 速度（秒）----
const TICK_OPENER: float = 1.5
const TICK_MARKER: float = 1.5
const TICK_DETECTOR: float = 1.5

# ---- 矿工 ----
const MINER_CAPACITY: int = 25
const MINER_MINE_PER_TICK: int = 10
const VEIN_RESOURCES: int = 250

# ---- 价格递增 ----
const PRICE_MULTIPLIER: float = 1.4

static func robot_price(base: int, count: int) -> int:
    return int(base * pow(PRICE_MULTIPLIER, count))

static func building_price(base: int, count: int) -> int:
    return int(base * pow(PRICE_MULTIPLIER, count))

# ---- 关卡目标 ----
static func level_target(N: int) -> int:
    # a + b*N + c*N^1.5
    return int(100 + 50 * N + 15 * pow(N, 1.5))

# ---- 局外升级成本 ----
const UPGRADES = {
    "start_money":       {"base": 10, "mult": 1.5, "max_lv": 5},
    "start_lives":       {"base": 20, "mult": 1.6, "max_lv": 3},
    "start_robot":       {"base": 40, "mult": 1.7, "max_lv": 2},
    "unlock_detector":   {"base": 50, "mult": 1.0, "max_lv": 1},
    "unlock_miner":      {"base": 50, "mult": 1.0, "max_lv": 1},
    "global_speed":      {"base": 30, "mult": 1.6, "max_lv": 3},
    "expand_map":        {"base": 50, "mult": 1.7, "max_lv": 3},
    "unlock_charge_tower": {"base": 100, "mult": 1.0, "max_lv": 1},
    "unlock_drone":      {"base": 80, "mult": 1.0, "max_lv": 1},
}

static func upgrade_cost(item_id: String, current_lv: int) -> int:
    var cfg = UPGRADES[item_id]
    return int(cfg.base * pow(cfg.mult, current_lv))

# ---- 矿石转化 ----
const ORE_DIVISOR: int = 10
const FAIL_BONUS_RATIO: float = 0.05
const MIN_ORE_PER_GAME: int = 10

static func ore_earned(score: int, passed: bool, target: int) -> int:
    var base_ore = score / ORE_DIVISOR
    var bonus = 0
    if not passed:
        bonus = int(target * FAIL_BONUS_RATIO)
    return max(base_ore + bonus, MIN_ORE_PER_GAME)
```

---

## 9. 待解决问题（v0.2 模型需要回答）

1. **marker 奖励削弱后是否还需要其他补偿？**（防止 marker 性价比低于 opener）
2. **关卡 N 的地图大小是否应该变？**（当前固定 16×16，建议 16→18→20→22）
3. **地雷密度是否随关卡上升？**（12.5% → 14% → 16%）
4. **充能塔/无人机解锁后的数值如何？**（待 P9/P10 实现后建模）
5. **声望/转生系统是否引入？**（解决长线饱和问题）
6. **多人/社交元素？**（微信小游戏的留存关键，但增加复杂度）

---

## 10. 模型局限性

1. **玩家行为假设**：v_player = 0.6 是估计值，需要实测校准
2. **机器人协同未建模**：实际多机器人会有路径冲突、目标锁定等损耗
3. **solver 卡死未建模**：扫雷猜雷问题会让玩家产出在游戏中段骤降（需观察）
4. **产业链 ROI 未单独建模**：当前简化为线性产出，实际是延迟反馈
5. **未考虑广告/付费加速**：商业化层未进入模型

建议：每跑 10 局实测后，回填实际数值到模型 A，重新校准其他模型。
